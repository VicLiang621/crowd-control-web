language: node_js

services:
    - postgresql

before_script:
    -psql -c 'create database test;' -U postgres
    -node models/db.js
    -node helper/populatedb.js

script:
    -mocha unittests.js --exit

install:
    -npm install 
